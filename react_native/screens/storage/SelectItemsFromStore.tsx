import { Text, View } from 'react-native';

import { SelectItemsFromStoreProps } from '../screen-types';

export default function SelectItemsFromStore({ navigation }: SelectItemsFromStoreProps) {
  return (
    <View>
      <Text>Tételek választása</Text>
    </View>
  );
}
