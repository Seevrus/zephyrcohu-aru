export type BaseItemType = {
  id: number;
  articleNumber: string;
  name: string;
  shortName: string;
  unitName: string;
  vatRate: string;
  netPrice: number;
  createdAt: string; // UTC
  updatedAt: string; // UTC
};
